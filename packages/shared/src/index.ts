/**
 * @crossword/shared
 *
 * Shared types and Zod schemas for the crossword app.
 * Used by both server and client packages.
 */

// Placeholder exports - will be populated in Milestone 2
export const VERSION = '0.0.1';

// Re-export Zod for convenience
export { z } from 'zod';
